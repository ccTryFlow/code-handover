# Contributing

感谢你愿意改进 CodeHandover。这个项目的目标是生成可信、可复核、便于人工补充的代码交接文档，因此每次改动都应尽量保守、可验证。

## 开发流程

1. 从最新代码创建分支。
2. 安装依赖：`npm install`。
3. 开发运行：`npm run dev`。
4. 核心链路测试：`npm run test:core`。
5. 提交前运行：`npm run check`。

## 代码约定

- TypeScript 开启严格检查，避免新增 `any`，除非 Electron IPC 边界暂时无法表达。
- 分析器优先返回结构化数据，不把展示文案混入底层扫描逻辑。
- 文档生成逻辑必须避免夸大静态分析结论。不能确认的内容应提示人工补充。
- 不提交生成的交接文档、构建产物、日志、密钥或 `.env`。
- 涉及 Git 作者范围、方法抽取或 Markdown 输出的改动，必须更新或补充 `scripts/test-core.cjs`。

## 新增语言或框架识别

新增识别能力时，请至少补齐：

- 框架检测依据和置信度。
- 模块扫描规则。
- 关键入口、路由、命令、模型或配置的提取规则。
- 对应的文档输出字段。
- 一个真实小项目或最小样例的人工验证说明。

## 安全注意

CodeHandover 会读取本地仓库文件并生成文档。请确保改动不会把以下内容写入日志或默认输出：

- API Key、Token、密码、私钥。
- 生产数据库地址和账号。
- 客户数据或业务敏感数据。
- 私有仓库凭据。
